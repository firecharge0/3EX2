#include "counter.h"

double tangents[1000000];
void* Counter(void* param) {
  double value = 0;
  if ((struct holder*)param.id = 1) {
    for (int i = 0; i < 200000; i++) {
      value += (holder*)*param.tans[i];
    }
  }
}